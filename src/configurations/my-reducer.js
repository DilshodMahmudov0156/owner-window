const initialState = {
    data: [],
    products: [],
    orders: [],
    messages: []
}

const MyReducer = (state = initialState, action) => {
    switch (action.type){
        default:
            return state;
    }
}

export default MyReducer;